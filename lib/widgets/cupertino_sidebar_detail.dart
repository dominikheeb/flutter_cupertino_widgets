import 'package:flutter/cupertino.dart';

class SidebarDetail extends StatelessWidget {
  const SidebarDetail({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // todo add header with top bar logic
    return Text("data");
  }
}
